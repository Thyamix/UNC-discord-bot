# It's a discord bot

I really doubt anyone would have a use for this, I made it because I needed it.

I have removed hard coded ids, so if you do want to use it you now can. Make sure to update the config.toml file. Also set "BOT_TOKEN" env var.